# Introduction to Ansible

In the follwoing repository you can find examples from the session as well as extra ones.

To run the playbooks you'll first have to create 2 or 3 vm nodes for ansible to manage.

You can use vagrant or just virtualbox to create vm nodes and an ansible vm from which you will run the commands.

